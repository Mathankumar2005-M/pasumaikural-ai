services:
  - type: web
    name: pasumai-kural-ai
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    healthCheckPath: /
    envVars:
      - key: ANTHROPIC_API_KEY
        value: ""
